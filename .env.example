# Copy this file to .env and fill in your MongoDB connection string.
# Do NOT commit your real credentials into git.
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/bakery?retryWrites=true&w=majority

# Example local fallback (used only if MONGODB_URI is not set):
# MONGODB_URI=mongodb://localhost:27017/bakery